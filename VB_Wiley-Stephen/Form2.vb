Public Class Form2
    REM for next semester: use the file add command to make the new form.
    REM I also could have started on this if you had drawn a UML diagram or described what was needed
    REM before you started building the UI on your machine
    Private Sub Form2_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        Form1.Close()
    End Sub
End Class